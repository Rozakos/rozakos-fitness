# Python
__pycache__/
*.pyc
.pytest_cache/
backend/*.db

# Node / Expo (mobile/ has its own .gitignore from the template)
node_modules/

# Env
.env

# Release builds (kept locally / attached to GitHub releases, not committed)
*.apk
